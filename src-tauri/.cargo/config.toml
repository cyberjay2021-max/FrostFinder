# rustc segfaults (SIGSEGV) during macro expansion when given a very large
# single file + a long tauri::generate_handler! list, because the default
# stack is too small for the recursive AST traversal.
# 16 MB stack is sufficient for this codebase.
[env]
RUST_MIN_STACK = "16777216"
